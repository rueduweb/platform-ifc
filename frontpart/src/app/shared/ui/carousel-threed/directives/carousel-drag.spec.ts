import { CarouselDrag } from './carousel-drag';

describe('CarouselDrag', () => {
  it('should create an instance', () => {
    const directive = new CarouselDrag();
    expect(directive).toBeTruthy();
  });
});
